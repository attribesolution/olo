<% if @orders.any? %>
  gon.lastOrderId = <%= @orders.first.id %>;
  <% @orders.reverse.each do |order| %>
      $('div.projects').prepend("<%= escape_javascript(render :partial => 'orders/item', :locals => { :order => order, :fullScreen => true }) %>");
  <% end %>
<% end %>