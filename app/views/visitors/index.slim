.hpanel
  .panel-body
    h2.font-light.m-b-xs
      i.fa.fa-internet-explorer aria-hidden="true" 
      | Orders
    small Listing Orders

- if @orders.any?  
  .row.projects
    - @orders.each do |order|
      = render "orders/item", order: order, fullScreen: false
